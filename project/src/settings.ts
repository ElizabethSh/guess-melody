export enum AppRoute {
  LOGIN = '/login',
  LOSE = '/lose',
  RESULT = '/result',
  ROOT = '/',
  // NOTE: Temporary routes
  ARTIST = '/artist',
  GENRE = '/genre'
}

export enum AuthorizationStatus {
  AUTH = 'auth',
  NO_AUTH = 'no-auth',
  UNKNOWN = 'unknown'
}
